appId: cc.polv.solfeggio
productName: solfeggio
mac:
  category: public.app-category.education
win:
  target:
    - target: nsis
    - target: portable
    - target: web
linux:
  target:
    - target: appImage
    - target: deb
      # to build rpm, executable rpmbuild is required, please install: sudo apt-get install rpm  
      # ArchLinux: rpm-tools
    - target: rpm
